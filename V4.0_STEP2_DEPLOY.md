# V4.0 STEP 2 — 上线（部署生产）操作指令

> 前提：STEP 1（V4.0_STEP1_CODEX.md）已由 Codex 完成并报告 **V4.0 LOCAL FINAL PASS**，且你已经人工核对下方上线前清单。
> 本步骤**不是** STEP 1 同一任务的延续，由你本人（或单独一次授权）逐项确认后执行。
> 前提不满足时，不要执行本步骤。

---

## 一、上线前核对（先在本地/预览确认）

- [ ] 本地验证输出：182 页、8 个 active 系列页、UEL/UK 404、sitemap 无 `uel` / `uk` / `ucp` / `ucf` 等 URL。
- [ ] 浏览器抽查：
  - `/en/products/`（三个新分类）
  - `/en/products/pillow-block-bearing-units/uc/`（含 UC 图 + ALT）
  - `/en/products/bearing-housing-series/p/`
  - `/ar/products/bearing-housing-series/p/`（RTL）
  - 页脚联系方式显示
- [ ] 页脚联系方式是否已换成企业信息（当前仍为个人 gmail / 微信 / 个人名，会公开显示；建议上线前换成 `info@natermanufacture.com` 等确认数据）。
- [ ] 决定 `noindex, follow` 是否保持（V4.0 默认保持；若决定收录，需另行处理 robots / sitemap / meta 开关——这是单独决策，不在本指令内默认执行）。

## 二、生产现状核对（部署前必须先查）

- [ ] 访问 `https://www.natermanufacture.com/en/products/`，记录当前生产产品结构（旧结构可能是 `Bearings / Bearing Housing / Custom` 或 `Mounted Spherical Roller Bearings / Custom`）。
- [ ] 记录旧 URL 清单（如 `/en/products/mounted-spherical-roller-bearings/…`、`/en/products/bearings/…`），用于上线后检查。

## 三、部署（二选一）

### 方式 A：Vercel Git 集成（推荐；仓库已配 vercel.json：buildCommand=`npm run build`，outputDirectory=`dist`）

1. 本地确认后执行（需你本人操作）：
   ```powershell
   Set-Location "D:\workspace\project_012_bearing_housing_website"
   git add -A
   git commit -m "V4.0 product structure: Pillow Block Bearing Units / Bearing Housing Series / Custom"
   git push
   ```
2. `git push` 到 Vercel 关联分支 → 触发自动构建部署。
3. 在 Vercel Dashboard 确认部署成功、Build Log 无错误。

### 方式 B：Vercel CLI

```powershell
Set-Location "D:\workspace\project_012_bearing_housing_website"
npm run build
npx vercel --prod
```

## 四、上线后验证（生产环境）

- [ ] `https://www.natermanufacture.com/en/products/` 显示三个新分类：Pillow Block Bearing Units / Bearing Housing Series / Custom。
- [ ] `/en/products/pillow-block-bearing-units/uc/` 返回 200，含 UC 图 + ALT。
- [ ] `/en/products/bearing-housing-series/p/` 返回 200。
- [ ] `/en/products/pillow-block-bearing-units/uel/` 与 `/uk/` 返回 404（pending 未生成）。
- [ ] `/en/products/mounted-spherical-roller-bearings/` 处理方式确认：noindex 阶段可接受 404；如需 301，需另改 `vercel.json` 加 redirects（V4.0 §13 默认不改 Vercel 配置，此为额外决策）。
- [ ] `https://www.natermanufacture.com/sitemap.xml` 无旧装配体/型号 URL。
- [ ] 抽样 2–3 个语言（en、ar、ja）的 hreflang / canonical 正确指向 `natermanufacture.com`。

## 五、回滚预案

- 保留部署前的最后一个成功 commit / 版本。
- 若生产异常：在 Vercel Dashboard 执行「Immediate Rollback」回滚到上一版本（旧结构会短暂恢复，属预期）；或 `git revert` 上一个 commit 后重新 push。
- 回滚后重新执行第四节验证，确认站点可用。

## 六、上线后归档

- 在 `PROGRESS.md` 记录上线时间、版本、验证结果。
- 旧 `mounted-spherical-roller-bearings` / `bearings` / `bearing-housing` URL 的处理方式（404 或 301）记入决策文档，供 SEO 后续跟进。

---

## 提醒

本步骤所有动作（commit / push / deploy / 改生产）都需你本人逐项确认后执行。Codex 只执行 STEP 1，交付报告即止。

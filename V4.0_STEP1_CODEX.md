# V4.0 STEP 1 — Codex 本地改造任务书（不部署）

> 任务对象：`D:\workspace\project_012_bearing_housing_website`
> 目标：把产品结构改造为 V4.0 冻结结构，完成本地验证。
> 全程只做本地「修改 → Build → Validate → 测试 → 报告」。
> **禁止 commit / push / deploy / 修改生产环境。**

---

## 一、目标结构（唯一依据，不得自行改动）

```
PRODUCTS
├── Pillow Block Bearing Units        slug: pillow-block-bearing-units
│   ├── UC Series    → active，图 uc-product.webp
│   ├── UEL Series   → pending，无图，不渲染
│   └── UK Series    → pending，无图，不渲染
├── Bearing Housing Series            slug: bearing-housing-series
│   ├── P / PA / F / FL / FC / FS / T Series → 全部 active，图 p/pa/f/fl/fc/fs/t-housing.webp
└── Custom                            slug: custom（无 series / 无图 / 无型号）
```

规则（V4.0 冻结）：
- 每个 Series 页只显示「Series Name + Series Image」。
- 禁止三级型号页 / 参数表 / Product Schema。
- `noindex, follow` 保持不变，不自行改成 index。
- 13 语言架构不变；系列代码不翻译；ALT 按语言生成。

---

## 二、执行步骤

### 0. 核对现状（必须先做）

- `git status` / `git diff` 查看未提交修改；发现与任务无关的未知修改，先报告再动手。
- 重读以下文件，以最新内容为准（下文行号为参考，可能偏移）：
  - `data/product-schema.mjs`
  - `data/product-catalog.mjs`
  - `scripts/build-pages.mjs`
  - `scripts/validate-site.mjs`
  - `scripts/validate-product-catalog.mjs`
  - `tests/product-seo.e2e.mjs`、`tests/responsive-v35.e2e.mjs`、`tests/international-seo.e2e.mjs`
- 发现其他文件仍引用 `retainedSourceSeries` / `MOUNTED_UNIT_ASSEMBLY_RULES` / `Mounted Spherical` 时，**停下报告，不要自行绕开**。

### 1. 归档废弃装配体图

- 创建 `assets/source-products/legacy-product-images/`。
- 把 `assets/product-images/` 下这 7 张图移入归档目录：
  - `ucp-pillow-block-bearing.webp`
  - `ucf-flange-bearing.webp`
  - `ucfl-bearing.webp`
  - `ucfl-reference-2-upright.webp`
  - `ucfc-round-flange-bearing.webp`
  - `uct-take-up-bearing.webp`
  - `uk-product.webp`
- 移动后 `assets/product-images/` 只保留 8 张：`uc-product.webp` + `p/pa/f/fl/fc/fs/t-housing.webp`。

### 2. 重写 `data/product-schema.mjs`

- 常量替换：
  ```js
  export const PRODUCT_CATEGORIES = ['Pillow Block Bearing Units', 'Bearing Housing Series', 'Custom'];
  export const SERIES_BY_CATEGORY = {
    'Pillow Block Bearing Units': ['UC', 'UEL', 'UK'],
    'Bearing Housing Series': ['P', 'PA', 'F', 'FL', 'FC', 'FS', 'T']
  };
  export const ACTIVE_SERIES = ['UC', 'P', 'PA', 'F', 'FL', 'FC', 'FS', 'T'];
  export const PENDING_SERIES = ['UEL', 'UK'];
  ```
- 删除 `MOUNTED_UNIT_ASSEMBLY_RULES`（原 L18–23）与 `assertAssembly()`（原 L78–85）。
- `schemaVersion` 断言改为 `'4.0.0'`。
- 新增 `validateSeries()`，校验 V4.0 系列 7 字段 + 追溯：
  - `category` ∈ SERIES_BY_CATEGORY 且 code ∈ 对应清单
  - `seriesCode` / `seriesName`（`${code} Series`）/ `description`（可为空 string）
  - `status` ∈ `active | pending`
  - active：必须带 `image`（WebP/AVIF、正整数宽高）、`alt` 必含 seriesCode
  - pending：`image` 必须为 null（防伪造图）、`alt` 为空
  - `sourceReference.document` / `.locator` 必填（内部追溯）
- 重写 `validateProductCatalog()`：
  - schemaVersion === '4.0.0'
  - 3 个固定分类必须存在、不得重复
  - 每分类 series 数量与顺序按 `SERIES_BY_CATEGORY` 固定断言
  - `category.products` 必须为空数组（V4.0 禁止任何三级型号页）
  - Custom 分类不得有 series
  - 删除全部「Mounted Spherical Roller Bearings」硬编码

### 3. 重写 `data/product-catalog.mjs`

- 删除 workbook 常量与旧 `workbookSeries` / `userConfirmedSeries` / `retainedSourceSeries` 实现。
- 新增统一构造器：
  ```js
  const userConfirmedSeries = (category, seriesCode, image) => ({
    category,
    seriesCode,
    seriesName: `${seriesCode} Series`,
    image: image ?? null,
    alt: image ? image.alt : '',
    description: '',
    status: image ? 'active' : 'pending',
    sourceReference: { document: 'V4.0 user-confirmed structure', locator: `Series=${seriesCode}`, fields: ['Series Name'] }
  });
  ```
- `productCatalog`：
  ```js
  {
    schemaVersion: '4.0.0',
    categories: [
      { category: 'Pillow Block Bearing Units', slug: 'pillow-block-bearing-units', images: [], series: [UC(active), UEL(pending), UK(pending)], products: [], publish: true },
      { category: 'Bearing Housing Series', slug: 'bearing-housing-series', images: [], series: [P, PA, F, FL, FC, FS, T 全部 active], products: [], publish: true },
      { category: 'Custom', slug: 'custom', images: [], series: [], products: [], publish: true, statement: 'Custom non-standard size products.' }
    ],
    retainedSourceSeries: [],
    unmappedSourceSeries: []
  }
  ```
- 图片映射（用现有文件）：
  - UC → `/assets/product-images/uc-product.webp`（223×248）
  - P/PA/F/FL/FC/FS/T → 对应 `*-housing.webp`（1000×1000）
  - UEL/UK → null（pending）
- `categories` 导出映射同步新 series 字段；`customProducts` 为空数组。

### 4. 同步 `scripts/build-pages.mjs`

- `seriesPath`（原 L151–153）：用 `series.seriesCode.toLowerCase()` 生成 URL。
- `seriesImage`（原 L256–260）：`series.image` 为 null 返回空；ALT 优先复用现有 `displayItemAlts[locale][series.seriesCode]`（已含 10 系列 ×13 语言 ALT），否则用 `series.alt`。
- `categoryCardPresentation`（原 L262–265）：改为三个新 slug：
  ```js
  {
    'pillow-block-bearing-units': { title: 'Pillow Block Bearing Units', visual: 'PILLOW BLOCK' },
    'bearing-housing-series': { title: 'Bearing Housing Series', visual: 'HOUSING SERIES' },
    custom: { title: 'Custom', visual: 'CUSTOM' }
  }
  ```
- `categoryCardImage`（原 L271–275）：取该分类第一个 `status === 'active' && series.image` 的系列。
- `categoryContent` / `seriesContent`（原 L313–326）：仅遍历 `status === 'active'` 系列；H1 = `${seriesCode} ${text.series}`；无图 pending 系列不生成入口。
- `buildProductsLocale`（原 L378–392）：仅 active 系列生成页面；Title = `${seriesCode} ${text.series} | ${category.title} | ${site.brand}`；Description = `${seriesCode} ${text.series}.`。
- dist 拷贝（原 L419–430）：公共产品图改为 `productCatalog.categories.flatMap(c => c.series.filter(s => s.status === 'active' && s.image).map(s => s.image.src.slice(1)))`。
- 若 `displayItemImage`（原 L251–254）已无调用，可删除。

### 5. 同步 `scripts/validate-site.mjs`

- `seriesRoutes`（原 L12–19）：只取 `status === 'active'` 的系列，字段改用 `seriesCode`（路由仍用 `seriesCode.toLowerCase()`）。
- `retainedProductAssets` / `deployedSeriesAssets`（原 L20–21）：改用 active 系列的 `series.image`。
- 新增 13 语言系列词映射（与 build 的 `text.series` 一致）：
  ```js
  const seriesWord = { en: 'Series', es: 'Serie', de: 'Serie', fr: 'Série', pt: 'Série', ar: 'السلسلة', tr: 'Seri', ru: 'Серия', it: 'Serie', vi: 'Dòng sản phẩm', id: 'Seri', ja: 'シリーズ', ko: '시리즈' };
  ```
- H1 校验（原 L247）：`h1 === \`${seriesRoute.code} ${seriesWord[locale]}\``。
- 系列图校验（原 L251–252）：`expectedImage = seriesRoute.record.image ?? null`。
- 分类硬编码（原 L309–314）：改为 `['Pillow Block Bearing Units', 'Bearing Housing Series', 'Custom']`；**禁用词追加** `Mounted Spherical Roller Bearings`、`Mounted Ball Bearing Units`、`Bearing Units`。
- 错误文案（原 L269）：`HOME, PRODUCTS and ABOUT US` → `HOME, PRODUCTS and CONTACT US`。
- 保留既有「dist 源文件泄露断言」与「未使用公共图断言」（归档图位于 `assets/source-products/`，应被现有正则豁免）。

### 6. 同步 `scripts/validate-product-catalog.mjs`

- 导入改为 `PRODUCT_CATEGORIES, SERIES_BY_CATEGORY, PRODUCT_MODEL_FIELDS`（去掉 `RETAINED_SOURCE_SERIES`）。
- 日志改为：`Validated V4.0 product data: 3 categories, 10 Series (8 active, 2 pending), 0 source-backed model pages.`

### 7. 同步 3 个 e2e 测试

- `tests/product-seo.e2e.mjs`：
  - 分类数组（原 L7–8）改为三个新分类 + 新 href。
  - 系列数组（原 L10–20）改为 10 个，含 `category`：
    ```js
    const series = [
      { code: 'UC',  image: true,  category: 'pillow-block-bearing-units' },
      { code: 'UEL', image: false, category: 'pillow-block-bearing-units' },
      { code: 'UK',  image: false, category: 'pillow-block-bearing-units' },
      { code: 'P',   image: true,  category: 'bearing-housing-series' },
      { code: 'PA',  image: true,  category: 'bearing-housing-series' },
      { code: 'F',   image: true,  category: 'bearing-housing-series' },
      { code: 'FL',  image: true,  category: 'bearing-housing-series' },
      { code: 'FC',  image: true,  category: 'bearing-housing-series' },
      { code: 'FS',  image: true,  category: 'bearing-housing-series' },
      { code: 'T',   image: true,  category: 'bearing-housing-series' }
    ].map(item => ({ ...item, name: `${item.code} Series`, href: `/en/products/${item.category}/${item.code.toLowerCase()}/` }));
    ```
  - 页面存在性断言（原 L53–59）：只对 `image: true` 系列断言 200；`image: false`（UEL/UK）断言 404。
  - 旧路由 404 断言（原 L76）：追加 `/en/products/mounted-spherical-roller-bearings/`；三级示例改为 `/en/products/pillow-block-bearing-units/uc/uc205/`。
  - 日志（原 L90）：改为「ten Series（eight active, two pending）」。
- `tests/responsive-v35.e2e.mjs`：
  - 路由（原 L18–22）换成新 slug：`/en/products/pillow-block-bearing-units/`、`/ar/products/bearing-housing-series/p/`、`/ko/products/custom/`。
  - 三级 404 示例（原 L223）改为 `/en/products/bearing-housing-series/p/p205/`。
- `tests/international-seo.e2e.mjs`：
  - 样例路由（原 L8）改为 `/ar/products/pillow-block-bearing-units/uc/`。

### 8. 更新文档（防误导，非功能）

- `claude.md`（原 L6–7）：PRODUCTS 层级改为 V4.0 冻结结构；删除「禁止恢复任何 Series」旧句，改为「可数据驱动扩展 Series（active/pending），禁止三级型号页」。
- `README.md`（原 L18–19）、`CONTEXT.md`（原 L12/16/28）、`NEXT_TASKS.md`（原 L5）：UCP/UCF…/UELP 表述改为 UC/UEL/UK + P/PA/…/T。
- `PROJECT_CONTEXT.md`（原 L13）、`DECISIONS.md`（原 L13）：`About Us` → `Contact Us`（事实错误，允许修）。
- `PROGRESS.md` 末尾追加一段 V4.0 改造记录。

---

## 三、验证（必须全部执行）

```powershell
Set-Location "D:\workspace\project_012_bearing_housing_website"
npm run build
npm run validate:data
npm run validate
node tests/product-seo.e2e.mjs
node tests/international-seo.e2e.mjs
node tests/responsive-v35.e2e.mjs
```

### 验收标准（全部满足才算 PASS）

1. 共 **182 页** = 13 语言 × 14 路由（`/`、`/products/`、3 个分类页、8 个 active 系列页、`/contact-us/`）。
2. UEL、UK **无页面、无 sitemap URL、访问返回 404**。
3. 前台无 `Bearings` / `Bearing Housing` / `Bearing Units` / `Mounted Ball` / `Mounted Spherical` 残留。
4. `dist/assets/product-images/` 仅 8 张启用图；7 张废弃图仅在 `assets/source-products/legacy-product-images/`。
5. 三级型号页 = 0；sitemap 无型号 URL。
6. 全站 `noindex, follow` 未变。

---

## 四、禁止项（全程）

- 不改 HOME / ABOUT / FOOTER / Contact Us / 联系方式 / 公司资料 / 域名 / Vercel 配置 / CSS 整体设计。
- 不 git commit / push，不 Vercel deploy，不改生产。
- 不新增三级型号页、不补技术参数、不用 AI 猜图补图（UEL/UK 保持 pending 直到人工提供确认图）。

---

## 五、交付报告（只报告，不部署）

按以下格式输出：

- **A. 已确认正确**
- **B. 已修复/改造**
- **C. 待人工确认**
- **D. 后续可补录 Series/图片**（列出 UEL、UK 缺图）
- **E. 测试结果**：Build / Validate Data / Validate / Products / International SEO / Responsive / URL-Routing / Old structure scan / Third-level URL scan 逐项
- **F. 最终状态**：全部通过 → **V4.0 LOCAL FINAL PASS**；否则只列真正阻碍项。

执行中若发现与本文档不符的既有硬编码（例如别处仍引用 `retainedSourceSeries`），先停下报告，不要自行绕开。
